function plotRLS(Pnorm)
figure('Position', [100, 100, 700, 650])
noteIdx = [5000, 12000, 20000];


semilogy(Pnorm);
hold on
grid on
box off
for i = 1:length(noteIdx)
    plot([noteIdx(i), noteIdx(i)], [1e2, 1e7], 'Color',[.5 .3 .2 .5])
end
end

